{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "relations-index.schema.json",
  "title": "Relations Index",
  "description": "Derived index of all relations in a FamilyTree dataset, stored at .familytree/relations-index.json",
  "type": "array",
  "items": {
    "type": "object",
    "required": ["uuid", "type"],
    "oneOf": [
      {
        "title": "filiation",
        "required": ["uuid", "type", "parent", "child"],
        "properties": {
          "uuid":   { "type": "string", "format": "uuid" },
          "type":   { "type": "string", "const": "filiation" },
          "parent": { "type": "string", "format": "uuid" },
          "child":  { "type": "string", "format": "uuid" }
        },
        "additionalProperties": false
      },
      {
        "title": "partner",
        "required": ["uuid", "type", "persons"],
        "properties": {
          "uuid":    { "type": "string", "format": "uuid" },
          "type":    { "type": "string", "const": "partner" },
          "persons": {
            "type": "array",
            "items": { "type": "string", "format": "uuid" },
            "minItems": 2,
            "maxItems": 2
          }
        },
        "additionalProperties": false
      },
      {
        "title": "association",
        "required": ["uuid", "type", "from", "to", "relation"],
        "properties": {
          "uuid":     { "type": "string", "format": "uuid" },
          "type":     { "type": "string", "const": "association" },
          "from":     { "type": "string", "format": "uuid" },
          "to":       { "type": "string", "format": "uuid" },
          "relation": { "type": "string" }
        },
        "additionalProperties": false
      }
    ]
  }
}
